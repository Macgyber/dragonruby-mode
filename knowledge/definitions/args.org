#+TITLE: 📘 DragonRuby Workbook: Args
#+AUTHOR: Sr. Esteban
#+OPTIONS: toc:2 num:nil
#+STARTUP: overview indent

* 🧭 Index
  - [[*Introduction][Introduction]]
  - [[*Key Concepts][Key Concepts]]
  - [[*Architecture][Architecture]]
  - [[*Base Code][Base Code]]
  - [[*Experiments][Experiments]]
  - [[*Logbook][Logbook]]

* 🌱 Introduction
  This document is a living world about DragonRuby's main object: *Args*.
  Notes are not just taken here: understanding is built on how data flows in the game.

* 🧠 Key Concepts
** Args
   The heart of the game loop. It is the container that DragonRuby passes to you in every `tick`.
   #+begin_quote
   “Everything the engine knows about the world is inside args.”
   #+end_quote

** Properties
   - state :: Your persistent database.
   - inputs :: The player's voice.
   - outputs :: The screen and the sound.

* 🏗 Architecture
** The Tick Cycle
   1. DragonRuby prepares `args`.
   2. Your code reads `args.inputs` and `args.state`.
   3. Your code writes to `args.outputs`.
   4. DragonRuby renders the frame.

* 🧪 Base Code
** Basic structure
#+begin_src ruby
def tick args
  args.outputs.labels << [640, 360, "Hello World", 5, 1]
end
#+end_src

* 🧬 Experiments
** Debugging args
   What happens if we print `args.state` every second?

* 📅 Logbook
** 2026-01-12
   Today we activated the knowledge engine. The documentation now breathes.
